window.innerWidth < 768 &&
  [].slice
    .call(document.querySelectorAll("[data-bss-disabled-mobile]"))
    .forEach(function (t) {
      t.classList.remove("animated"),
        t.removeAttribute("data-bss-hover-animate"),
        t.removeAttribute("data-aos"),
        t.removeAttribute("data-bss-parallax-bg"),
        t.removeAttribute("data-bss-scroll-zoom");
    }),
  document.addEventListener("DOMContentLoaded", function () {}, !1);
let i = 2;
$(document).ready(function () {
  var t,
    e = $(".itemDot"),
    a = $(".dotCircle"),
    s = a.width();
  t = s / 2.5;
  var o = a.height(),
    r = 0,
    n = (2 * Math.PI) / e.length;
  e.each(function () {
    var e = Math.round(s / 2 + t * Math.cos(r) - $(this).width() / 2),
      a = Math.round(o / 2 + t * Math.sin(r) - $(this).height() / 2);
    window.console && console.log($(this).text(), e, a),
      $(this).css({ left: e + "px", top: a + "px" }),
      (r += n);
  }),
    $(".itemDot").click(function () {
      var t = $(this).data("tab");
      $(".itemDot").removeClass("active"),
        $(this).addClass("active"),
        $(".CirItem").removeClass("active"),
        $(".CirItem" + t).addClass("active"),
        (i = t),
        $(".dotCircle").css({
        //   transform: "rotate(" + (360 - 36 * (i - 1)) + "deg)",
          transition: "2s",
        }),
        $(".itemDot").css({
          transform: "rotate(" + 36 * (i - 1) + "deg)",
          transition: "1s",
        });
    }),
    setInterval(function () {
      var t = $(".itemDot.active").data("tab");
      (t > 6 || i > 6) && ((t = 1), (i = 1)),
        $(".itemDot").removeClass("active"),
        $('[data-tab="' + i + '"]').addClass("active"),
        $(".CirItem").removeClass("active"),
        $(".CirItem" + i).addClass("active"),
        i++,
        $(".dotCircle").css({
          transform: "rotate(" + (360 - 36 * (i - 2)) + "deg)",
          transition: "2s",
        }),
        $(".itemDot").css({
          transform: "rotate(" + 36 * (i - 2) + "deg)",
          transition: "1s",
        });
    }, 5e3);
});
